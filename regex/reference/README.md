# Advanced Regex Notes

This subfolder keeps the deeper regex reference close to the practical examples in the parent `regex/` folder.

## In This Folder

- [REGEX_REFERENCE.md](REGEX_REFERENCE.md)

Use the parent folder first if you are still learning the basics of `re.search()`, `re.findall()`, and `re.sub()`.
